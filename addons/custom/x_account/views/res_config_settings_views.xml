<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_x_account_settings" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="context">{'module': 'x_account', 'bin_size': False}</field>
    </record>

    <record id="x_account_res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.x.account</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="X Account" string="X Account" name="x_account" groups="base.group_system">
                    <block title="Authentication">
                        <setting string="Authentication Method" help="Authentication method used when adding X accounts.">
                            <field name="x_auth_method"/>
                        </setting>
                    </block>

                    <block title="Providers">
                        <setting string="Provider" help="Provider implementation used for X HTTP operations.">
                            <field name="x_provider"/>
                        </setting>
                        <setting string="Event Provider" help="Provider for webhooks, event subscriptions, and incoming event processing.">
                            <field name="x_event_provider"/>
                        </setting>
                        <setting string="Action Provider" help="Provider for mutations (retweet, reply, like, follow, send DM, etc.) and data reads.">
                            <field name="x_action_provider"/>
                        </setting>
                    </block>

                    <block title="Advanced">
                        <setting string="Web API Base URL">
                            <field name="x_web_api_base"/>
                        </setting>
                        <setting string="Web Bearer Token" help="Bearer token used by the undocumented web-session provider. Stored encrypted/out of direct exposure; used only by the provider.">
                            <field name="x_web_bearer_token" password="True"/>
                        </setting>
                        <setting string="Session Encryption Key Configured" help="Indicates that the encryption key is supplied via deployment config (X_SESSION_ENCRYPTION_KEY). The key itself is never stored in Odoo.">
                            <field name="x_encryption_key_configured"/>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>
</odoo>