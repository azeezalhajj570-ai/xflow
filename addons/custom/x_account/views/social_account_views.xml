<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="x_account_social_account_view_form" model="ir.ui.view">
        <field name="name">x.account.social.account.view.form</field>
        <field name="model">social.account</field>
        <field name="inherit_id" ref="social.social_account_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='social_account_handle']" position="attributes">
                <attribute name="readonly">1</attribute>
            </xpath>
            <xpath expr="//group[@name='social_account_global']" position="after">
                <notebook>
                    <page string="Settings" name="x_account_info">
                        <group name="x_account_connection" col="1">
                            <field name="twitter_user_id" readonly="1"/>
                            <!-- Providers inject their credentials just under the
                                 X user id (auth token, ct0, ...). -->
                            <field name="x_chat_key_mode" widget="radio"/>
                            <field name="x_encryption_code" password="True"
                                placeholder="Your XChat PIN"
                                invisible="x_chat_key_mode != 'juicebox'"/>
                            <field name="x_auto_archive" help="Archive this account automatically once a day inside the window below."/>
                            <field name="x_auto_archive_start" widget="float_time" options="{'type': 'time'}" required="x_auto_archive" invisible="not x_auto_archive"/>
                            <field name="x_auto_archive_end" widget="float_time" options="{'type': 'time'}" required="x_auto_archive" invisible="not x_auto_archive"/>
                            <field name="x_chat_key_blob" widget="text"
                                invisible="x_chat_key_mode != 'key_blob'"/>
                        </group>
                        <!-- Filled by the provider modules: the subscription
                             state lives in x_account_twitter. -->
                        <group name="x_account_status_subscription" string="Subscriptions"/>
                        <!-- Providers add their own connection settings here
                             (provider credentials, OmniX webhook fields). -->
                        <group name="x_account_api"/>
                    </page>
                    <!-- Hidden: the account health is reported by the status bar
                         (aggregated x_connection_status). Kept for the provider
                         xpaths and for deep diagnosis. -->
                    <page string="Status" name="x_account_status" invisible="1">
                        <group>
                            <group name="x_account_status_connection" string="Authentication">
                                <field name="x_auth_method" readonly="1"/>
                                <field name="x_provider" readonly="1"/>
                                <field name="x_event_provider" readonly="1"/>
                                <field name="x_action_provider" readonly="1"/>
                                <field name="x_session_store_id" readonly="1"/>
                                <field name="last_connected" readonly="1"/>
                                <field name="last_validated" readonly="1"/>
                            </group>
                            <group name="x_account_status_encryption" string="Chat Encryption">
                                <field name="x_chat_key_mode" readonly="1"/>
                                <field name="x_chat_initialized" readonly="1" widget="boolean_toggle"/>
                                <field name="x_chat_pin_locked" readonly="1" widget="boolean_toggle"/>
                                <field name="x_chat_signing_key_version" readonly="1"/>
                                <field name="x_chat_decrypt_stopped" readonly="1" widget="boolean_toggle"/>
                                <field name="x_chat_decrypt_fail_streak" readonly="1"/>
                                <field name="x_chat_decrypt_notified_at" readonly="1"/>
                                <field name="x_chat_key_fetch_failed_at" readonly="1"/>
                            </group>
                        </group>
                        <group>
                            <group name="x_account_status_error">
                                <field name="last_error" readonly="1" nolabel="1" colspan="2"
                                    placeholder="No error recorded"/>
                            </group>
                        </group>
                    </page>
                </notebook>
            </xpath>
            <xpath expr="//div[hasclass('oe_title')]/h1" position="before">
                <field name="active" widget="boolean_toggle"/>
            </xpath>
            <xpath expr="//header" position="inside">
                <!-- The buttons that used to sit here are server actions bound
                     to social.account, offered in the form's Action menu; see
                     data/server_actions_form.xml. -->
                <field name="x_connection_status"
                    widget="statusbar"
                    readonly="1"
                    statusbar_visible="not_configured,active,error"/>
            </xpath>
            <xpath expr="//form" position="inside">
                <chatter/>
            </xpath>
        </field>
    </record>

    <record id="x_account_social_account_view_form_standalone" model="ir.ui.view">
        <field name="name">x.account.social.account.view.form.standalone</field>
        <field name="model">social.account</field>
        <field name="arch" type="xml">
            <form string="X Account" create="false">
                <header>
                    <!-- The actions that used to be buttons here are bound
                         server actions; see data/server_actions_form.xml. -->
                    <field name="x_connection_status"
                        widget="statusbar"
                        readonly="1"
                        statusbar_visible="not_configured,active,error"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <field name="active" widget="boolean_toggle"/>
                        <h1><field name="name" placeholder="Account name"/></h1>
                    </div>
                    <notebook>
                        <page string="Settings" name="x_account_info">
                            <group name="x_account_connection" col="1">
                                <field name="social_account_handle" placeholder="@handle" readonly="1"/>
                                <field name="twitter_user_id" readonly="1"/>
                                <!-- Providers inject their credentials just under the
                                     X user id (auth token, ct0, ...). -->
                                <field name="x_chat_key_mode" widget="radio"/>
                                <field name="x_encryption_code" password="True"
                                    placeholder="Your XChat PIN"
                                    invisible="x_chat_key_mode != 'juicebox'"/>
                                <field name="x_auto_archive" help="Archive this account automatically once a day inside the window below."/>
                                <field name="x_auto_archive_start" widget="float_time" options="{'type': 'time'}" required="x_auto_archive" invisible="not x_auto_archive"/>
                                <field name="x_auto_archive_end" widget="float_time" options="{'type': 'time'}" required="x_auto_archive" invisible="not x_auto_archive"/>
                                <field name="x_chat_key_blob" widget="text" placeholder="Paste key blob here..."
                                    invisible="x_chat_key_mode != 'key_blob'"/>
                            </group>
                            <!-- Filled by the provider modules: the subscription
                                 state lives in x_account_twitter. -->
                            <group name="x_account_status_subscription" string="Subscriptions"/>
                            <!-- Providers add their own connection settings here
                                 (provider credentials, OmniX webhook fields). -->
                            <group name="x_account_api"/>
                        </page>
                        <!-- Hidden: the account health is reported by the status bar
                             (aggregated x_connection_status). Kept for the provider
                             xpaths and for deep diagnosis. -->
                        <page string="Status" name="x_account_status" invisible="1">
                            <group>
                                <group name="x_account_status_connection" string="Authentication">
                                    <field name="x_auth_method" readonly="1"/>
                                    <field name="x_provider" readonly="1"/>
                                    <field name="x_event_provider" readonly="1"/>
                                    <field name="x_action_provider" readonly="1"/>
                                    <field name="x_session_store_id" readonly="1"/>
                                    <field name="last_connected" readonly="1"/>
                                    <field name="last_validated" readonly="1"/>
                                </group>
                                <group name="x_account_status_encryption" string="Chat Encryption">
                                    <field name="x_chat_key_mode" readonly="1"/>
                                    <field name="x_chat_initialized" readonly="1" widget="boolean_toggle"/>
                                    <field name="x_chat_pin_locked" readonly="1" widget="boolean_toggle"/>
                                    <field name="x_chat_signing_key_version" readonly="1"/>
                                    <field name="x_chat_decrypt_stopped" readonly="1" widget="boolean_toggle"/>
                                    <field name="x_chat_decrypt_fail_streak" readonly="1"/>
                                    <field name="x_chat_decrypt_notified_at" readonly="1"/>
                                    <field name="x_chat_key_fetch_failed_at" readonly="1"/>
                                </group>
                            </group>
                            <group>
                                <group name="x_account_status_error">
                                    <field name="last_error" readonly="1" nolabel="1" colspan="2"
                                        placeholder="No error recorded"/>
                                </group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="x_account_social_account_view_list" model="ir.ui.view">
        <field name="name">x.account.social.account.view.list</field>
        <field name="model">social.account</field>
        <field name="arch" type="xml">
            <list string="X Accounts" create="false">
                <field name="active" widget="boolean_toggle"/>
                <field name="name"/>
                <field name="social_account_handle"/>
                <field name="x_provider" column_invisible="1"/>
                <field name="x_connection_status" widget="badge"
                    decoration-success="x_connection_status == 'active'"
                    decoration-warning="x_connection_status == 'not_configured'"
                    decoration-danger="x_connection_status == 'error'"/>
                <!-- <field name="last_validated"/> -->
            </list>
        </field>
    </record>

    <record id="view_social_account_search_inherit_x_account" model="ir.ui.view">
        <field name="name">social.account.search.inherit.x_account</field>
        <field name="model">social.account</field>
        <field name="inherit_id" ref="social.social_account_view_search"/>
        <field name="arch" type="xml">
            <xpath expr="//filter[@name='filter_active']" position="after">
                <filter string="Active" name="x_filter_active"
                    domain="[('active', '=', True)]"/>
                <filter string="Include Archived" name="x_filter_include_archived"
                    context="{'active_test': False}"/>
            </xpath>
            <xpath expr="//search" position="inside">
                <separator/>
                <filter string="X: Connected" name="x_connection_active"
                    domain="[('x_connection_status', '=', 'active')]"/>
                <filter string="X: Not Configured" name="x_connection_not_configured"
                    domain="[('x_connection_status', '=', 'not_configured')]"/>
                <filter string="X: Error" name="x_connection_error"
                    domain="[('x_connection_status', '=', 'error')]"/>
                <filter string="X: Chat Not Initialized" name="x_chat_not_initialized"
                    domain="[('x_chat_initialized', '=', False)]"/>
                <filter string="X: PIN Rejected" name="x_chat_pin_locked"
                    domain="[('x_chat_pin_locked', '=', True)]"/>
                <filter string="X: Chat Decryption Stopped" name="x_chat_decrypt_stopped"
                    domain="[('x_chat_decrypt_stopped', '=', True)]"/>
                <group>
                    <filter string="X Connection Status" name="x_groupby_connection_status" domain="[]"
                        context="{'group_by': 'x_connection_status'}"/>
                    <filter string="X Chat Key Source" name="x_groupby_chat_key_mode" domain="[]"
                        context="{'group_by': 'x_chat_key_mode'}"/>
                    <filter string="X Chat Initialized" name="x_groupby_chat_initialized" domain="[]"
                        context="{'group_by': 'x_chat_initialized'}"/>
                </group>
            </xpath>
        </field>
    </record>

    <record id="action_x_account_social_accounts" model="ir.actions.act_window">
        <field name="name">X Accounts</field>
        <field name="res_model">social.account</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('media_type', '=', 'twitter')]</field>
        <field name="context">{'search_default_x_filter_include_archived': True}</field>
        <field name="view_ids" eval="[(5, 0, 0),
            (0, 0, {'view_mode': 'list', 'view_id': ref('x_account_social_account_view_list')}),
            (0, 0, {'view_mode': 'form', 'view_id': ref('x_account_social_account_view_form_standalone')})]"/>
    </record>
</odoo>
