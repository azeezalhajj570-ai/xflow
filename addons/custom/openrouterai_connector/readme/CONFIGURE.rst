1. Install the module and the base `ai` module
2. Navigate to OpenRouter AI > Configurations
3. Create a new provider record with your OpenRouter API key
4. Optionally set App URL and App Name for OpenRouter ranking
5. Click "Sync Models" to populate the model catalog
6. Go to OpenRouter AI > Models to browse available models
